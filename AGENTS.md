# AGENTS.md

## 0. このリポジトリでの最重要方針

このWebアプリは「動けばよい」ではなく、実際の業務現場で迷わず・壊さず・短時間で使えることを最優先にする。

コード変更時は、必ず以下の順で判断する。

1. 既存データを壊さない
2. 既存機能を壊さない
3. ユーザーの操作ミスを減らす
4. 画面上で現在の状態が分かる
5. 入力・保存・復元・出力が確実に動く
6. 見た目よりも、業務上の判断ミスを減らすUXを優先する

不明点があっても、作業を止めすぎない。既存コード・README・画面構成・実装済みの仕様から合理的に判断し、判断した前提を作業結果に明記する。

---

## 1. 作業開始時のルール

作業を始める前に必ず以下を確認する。

* 現在のアプリ構成
* 主要画面
* データ保存方式
* 入出力方式
* 既存のエラー表示・バリデーション
* 既存の命名規則
* 既存UIのトーン、余白、色、フォント、ボタン配置
* 既知の不具合やTODO

実装前に、変更対象のファイルと影響範囲を簡潔に整理する。

大きな変更では、いきなり実装しない。先に以下を整理する。

* 目的
* 変更対象
* 影響する画面
* 影響するデータ
* 既存機能へのリスク
* 動作確認方法

---

## 2. UX設計の原則

このアプリのUXでは、見た目の美しさよりも「業務で間違えないこと」を優先する。

### 2.1 状態が分かること

ユーザーが常に以下を把握できるようにする。

* 今どの画面にいるか
* 何を編集中か
* 未保存か保存済みか
* 確定済みか再編集可能か
* エラーがあるか
* エラーは未対応か許容済みか
* 操作が成功したか失敗したか
* 読み込み中か処理中か

状態は、色だけに依存しない。文字・アイコン・ラベル・枠線・説明文を併用する。

### 2.2 操作ミスを防ぐこと

以下の操作には確認を入れる。

* データ削除
* 確定解除
* 上書き保存
* バックアップ復元
* 大量データの一括変更
* エラーの許容
* 設定の初期化

確認ダイアログには、何が起きるかを具体的に書く。

悪い例:
「実行しますか？」

良い例:
「現在の入力内容をバックアップファイルの内容で上書きします。元に戻せない可能性があります。復元しますか？」

### 2.3 ユーザーに考えさせすぎないこと

ユーザーが毎回判断しなくてよいものは、アプリ側で補助する。

* 入力例を表示する
* 未入力時の扱いを明示する
* エラーの原因と修正方法をセットで出す
* 空欄・未設定・0件の意味を明示する
* 主要ボタンと補助ボタンの強弱を明確にする
* 似た操作名を避ける

### 2.4 エラー表示の原則

エラーは「怒る表示」ではなく「修正できる表示」にする。

エラー表示には必ず以下を含める。

* 何が問題か
* どこが問題か
* なぜ問題か
* どう直すか
* 許容できる種類のエラーか

例:
「6/16 山田さんが最大連勤数を超えています。前後の勤務を確認し、休みを入れるか、この連勤を許容してください。」

### 2.5 情報量の優先順位

画面に情報を追加するときは、以下の順に優先する。

1. 業務判断に必要な情報
2. ミス防止に必要な情報
3. 現在状態を示す情報
4. 操作説明
5. 装飾

装飾のために、業務判断に必要な情報を圧迫してはいけない。

---

## 3. 機能要件の扱い

機能を追加・修正するときは、以下を必ず定義する。

* 入力
* 処理
* 出力
* 成功条件
* 失敗条件
* エラー表示
* データ保存への影響
* 既存機能への影響
* テスト観点

仕様が曖昧な場合は、既存実装とユーザーの業務目的を優先して補完する。

ただし、以下は勝手に変更しない。

* データ構造
* 保存形式
* CSV/JSON/Excelなどの出力形式
* 既存の項目名
* 業務上意味を持つ文言
* 既存の色分けルール
* 既存の確定・保存・復元ロジック

変更が必要な場合は、理由と移行方法を明記する。

---

## 4. 実装方針

### 4.1 小さく安全に変更する

一度に広範囲を変更しない。

優先順位は以下。

1. 致命的な不具合修正
2. データ破損リスクの排除
3. 保存・復元・出力の安定化
4. UX上の迷いを減らす改善
5. 見た目の調整
6. リファクタリング

リファクタリングは、機能変更と混ぜない。混ぜる場合は、どこが機能変更でどこが整理なのかを明記する。

### 4.2 既存UIを尊重する

既存のトーン、配色、余白、フォント、角丸、ボタン配置に合わせる。

新しいUI部品を追加する場合は、既存コンポーネントの見た目に寄せる。

画面全体のデザインを勝手に作り替えない。

### 4.3 命名

名前は、業務利用者が理解できる言葉を優先する。

開発者向けの抽象名より、画面上の意味に近い名前を使う。

悪い例:

* processData
* handleItem
* flag
* mode2

良い例:

* restoreBackup
* validateShiftRules
* allowedErrorIds
* confirmedDepartmentSchedule

---

## 5. データ保護ルール

このアプリでは、ユーザーが入力したデータを失うことが最大の失敗である。

以下を必ず守る。

* 保存処理は成功・失敗を画面で明示する
* 復元処理は実行前に確認する
* 復元失敗時に既存データを消さない
* JSON読み込み時は形式検証を行う
* 不正なJSONを読み込んでもアプリを壊さない
* localStorageなど既存保存方式を勝手に変更しない
* データ構造を変える場合は後方互換を考慮する
* 出力ファイル名は内容と日時が分かる形にする

バックアップ・復元機能を変更した場合は、必ず以下を確認する。

* バックアップファイルを出力できる
* 出力されたファイルを再読み込みできる
* 読み込み後に画面へ反映される
* 読み込み後に再保存できる
* 不正ファイル読み込み時に既存データが残る

---

## 6. バリデーションとエラー許容

バリデーションは、ユーザーの作業を止めるためではなく、判断ミスを減らすために使う。

### 6.1 エラーの分類

エラーは可能な限り分類する。

* 重大: 業務上そのまま使えない
* 警告: 確認が必要
* 情報: 判断材料
* 許容済み: ユーザーが意図的に許容したもの

### 6.2 許容済みエラー

許容済みにしても、メッセージ自体は消さない。

許容済みの場合は以下を満たす。

* 未対応エラーと視覚的に区別できる
* ただし完全に見えなくしない
* どのエラーを誰が見ても許容済みと分かる
* チェックを外せば元の状態に戻る
* 関連するセルや期間がある場合は、それらも整合して表示する

### 6.3 エラー件数

エラー件数は、画面遷移しなくても確認できるようにする。

件数表示には以下を含める。

* 重大件数
* 警告件数
* 許容済み件数
* 未対応件数

---

## 7. アクセシビリティ

最低限、以下を守る。

* 文字サイズは小さすぎない
* コントラストを確保する
* 色だけで意味を伝えない
* ボタン名は動作が分かる文言にする
* アイコンだけのボタンにはテキストまたはaria-labelを付ける
* キーボード操作を極端に阻害しない
* 入力エラーは入力欄の近くに表示する
* ダイアログを閉じる方法を明確にする

---

## 8. レスポンシブと表示崩れ

PC利用を主対象にしても、画面幅が狭い場合に最低限壊れないようにする。

以下を確認する。

* 横スクロールが必要な表は、表の外側だけがスクロールする
* 固定ヘッダーや固定メニューがコンテンツを隠さない
* ボタンが折り返しても押せる
* モーダルが画面外にはみ出さない
* 印刷・PDF出力時に重要情報が切れない

---

## 9. パフォーマンス

体感速度を重視する。

以下の処理では、必要に応じて進捗・処理中表示を出す。

* 自動生成
* 大量バリデーション
* バックアップ復元
* CSV/Excel/PDF出力
* 一括反映

重い処理を追加する場合は、画面が固まって見えないようにする。

---

## 10. テスト・確認ルール

変更後は、可能な範囲で必ず確認する。

### 10.1 最低確認

* アプリが起動する
* コンソールエラーが出ない
* 変更対象画面が表示される
* 主要ボタンが反応する
* 保存・復元・出力に影響がない
* 既存データで壊れない

### 10.2 機能変更時の確認

機能を変更した場合は、以下を確認する。

* 正常系
* 異常系
* 未入力
* 0件
* 大量データ
* 境界値
* 既存データ読み込み
* 画面更新後の状態維持

### 10.3 UI変更時の確認

UIを変更した場合は、以下を確認する。

* 視認性
* 押し間違いのしにくさ
* ラベルの意味
* エラー時の分かりやすさ
* 画面幅変更時の崩れ
* 印刷・出力への影響

---

## 11. 完了条件

作業完了時には、以下を満たしている必要がある。

* 要件を満たしている
* 既存機能を壊していない
* データ破損リスクが増えていない
* 画面上で状態が分かる
* エラー時の挙動が定義されている
* 操作成功・失敗がユーザーに伝わる
* 主要な確認手順を実施している
* 変更内容と確認結果を報告できる

完了報告には必ず以下を書く。

* 変更した内容
* 変更したファイル
* 確認した動作
* 既知の未対応事項
* 影響範囲
* ユーザーが次に確認すべき点

---

## 12. 禁止事項

以下は禁止。

* 既存仕様を確認せずに全面作り替えする
* 指示されていない大規模リファクタリングを行う
* データ構造を黙って変更する
* 保存・復元・出力形式を黙って変更する
* エラーを非表示にして解決したように見せる
* console.logを大量に残す
* 不要な外部依存を追加する
* CDNや外部APIを勝手に追加する
* ユーザー入力データを外部送信する
* セキュリティ上危険な処理を説明なしに追加する
* 画面全体のトーンを勝手に変更する
* 業務用語を開発者都合で抽象化する

---

## 13. セキュリティ

Webアプリでは以下を守る。

* ユーザー入力をHTMLとして直接挿入しない
* innerHTMLを使う場合は入力値の扱いを明確にする
* 外部リンクは必要に応じてrel属性を付ける
* ローカル保存データに機密情報を含める場合は注意喚起する
* ファイル読み込みでは形式・サイズ・必須項目を検証する
* 例外発生時に内部情報を過剰表示しない
* 依存ライブラリを追加する場合は必要性を説明する

---

## 14. AIエージェントとしての振る舞い

作業中は以下を守る。

* まず既存実装を読む
* 既存の意図を推測し、壊さない
* 不明点はコード・画面・仕様から補完する
* 判断に迷う場合は安全側に倒す
* 複数案がある場合は、業務事故が少ない案を選ぶ
* 見た目の改善だけでなく、操作ミス・確認漏れ・復旧不能を減らす
* 変更後は自己レビューする
* ユーザーへの報告では、できたことだけでなくリスクも書く

---

## 15. UXレビュー観点

実装後、以下の観点で自己レビューする。

* 初見でも何をすればよいか分かるか
* 次に押すべきボタンが分かるか
* 危険な操作が危険に見えるか
* 成功したことが分かるか
* 失敗したときに復旧できるか
* エラー箇所までたどり着けるか
* 入力前・入力中・入力後の状態が分かるか
* 一覧・詳細・編集の関係が分かるか
* 同じ意味のボタン名が複数存在しないか
* 業務上の判断に必要な情報が隠れていないか

自己レビュー後、役割の違う5名にレビューしてもらう。
レビュー結果を基に議論し、PDCAを回してアプリを最適化する。

---

## 16. 変更報告テンプレート

作業完了時は、以下の形式で報告する。

### 変更内容

*

### 変更ファイル

*

### 動作確認

*

### UX上の改善点

*

### 既存機能への影響

*

### 未対応・注意点

*

### 次に確認すべきこと

*
